# LOCAL-ONLY override (auto-loaded by `docker compose`, NOT deployed to prod).
# The db is published on host 5432 by the base compose; here we just keep it
# auto-restarting across reboots for local dev.
services:
  db:
    restart: unless-stopped
