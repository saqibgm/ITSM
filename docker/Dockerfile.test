FROM python:3.12-slim
WORKDIR /app
RUN addgroup --system app && adduser --system --group app

COPY pyproject.toml .
RUN pip install --no-cache-dir ".[dev]"

COPY --chown=app:app . .
USER app

CMD ["pytest", "tests/", "-x", "-v", "--tb=short"]
